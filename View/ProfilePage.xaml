<Page x:Class="ReadMangaApp.View.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReadMangaApp.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfilePage">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border BorderThickness="1" CornerRadius="5" Padding="5" Background="#CCFFFFFF">
            <StackPanel>
                <TextBlock Text="Профиль пользователя:" Margin="5" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="{Binding Username}" Margin="5" Grid.Row="1" FontSize="14"/>
                <TextBlock Text="{Binding Email}" Margin="5" Grid.Row="2" FontSize="14"/>
                <Button Content="Редактировать профиль" Margin="5" Grid.Row="3" Command="{Binding EditProfileCommand}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
