<Page x:Class="ReadMangaApp.View.MainMangaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReadMangaApp.View"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="850"
      Title="MainMangaPage">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Border BorderThickness="1" CornerRadius="5" Padding="5" Background="#CCFFFFFF" Margin="0,0,3,0">
            <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Mangas}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel ItemWidth="185" ItemHeight="305" Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" CornerRadius="5" Margin="3" >
                                <StackPanel Margin="5">
                                    <Button Command="{Binding DataContext.ReadMangaCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}" Background="Transparent" BorderBrush="Transparent" Margin="0" Padding="0">
                                        <Grid>
                                            <Image MaxWidth="180" MaxHeight="240" Source="{Binding CoverUrl}" Stretch="Fill"/>
                                            <Border BorderThickness="1" CornerRadius="5" Margin="3" 
                                                Background="{Binding RatingBackground}"
                                                Padding="5" 
                                                HorizontalAlignment="Right" VerticalAlignment="Top" 
                                                Width="40" Height="25">
                                                <TextBlock Text="{Binding MangaScores.AverageScore, StringFormat={}{0:F1}}" FontSize="14" 
                                                    TextWrapping="Wrap" Foreground="White" FontWeight="Bold" 
                                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Button>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" TextWrapping="NoWrap" MaxWidth="290" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding TypeManga.Name}" FontSize="14" Margin="0,2,0,10" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding MangaScores.AverageScore}" FontSize="44" Margin="0,2,0,10" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
        </ScrollViewer>
        </Border>
        <Grid Grid.Column="1" Background="Transparent">
            <Border BorderThickness="1" CornerRadius="5" Padding="5" Background="#CCFFFFFF" VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="5" Grid.Row="0">
                        <StackPanel>
                            <Border BorderBrush="LightGray" BorderThickness="0,0,0,1">
                                <StackPanel>
                                    <Grid Margin="0,5" VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Жанры:" VerticalAlignment="Center" />
                                        <ToggleButton x:Name="GenreToggleButton" Grid.Column="1" Content="▼" Width="25" Height="25" Margin="5,0,0,0" VerticalAlignment="Center" Click="GenreToggleButton_Click"/>
                                    </Grid>
                                    <ItemsControl x:Name="GenreItemsControl" Visibility="Collapsed" Margin="0,5" ItemsSource="{Binding Genres}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,3"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                            <Border BorderBrush="LightGray" BorderThickness="0,0,0,1">
                                <StackPanel>
                                    <Grid Margin="0,5" VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Теги:" VerticalAlignment="Center"/>
                                        <ToggleButton x:Name="TegToggleButton" Grid.Column="1" Content="▼" Width="25" Height="25" Margin="5,0,0,0" VerticalAlignment="Center" Click="TegToggleButton_Click"/>
                                    </Grid>
                                    <ItemsControl x:Name="TegItemsControl" Visibility="Collapsed" Margin="0,5" ItemsSource="{Binding Tegs}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,3"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                            <Border BorderBrush="LightGray" BorderThickness="0,0,0,1">
                                <StackPanel>
                                    <Grid Margin="0,5" VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Издатели:" VerticalAlignment="Center"/>
                                        <ToggleButton x:Name="PublisherToggleButton" Grid.Column="1" Content="▼" Width="25" Height="25" Margin="5,0,0,0" VerticalAlignment="Center" Click="PublisherToggleButton_Click"/>
                                    </Grid>
                                    <ItemsControl x:Name="PublisherItemsControl" Visibility="Collapsed" Margin="0,5" ItemsSource="{Binding Publishers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,3"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>   
                    </ScrollViewer>
                    <Button Content="Сортировать" Command="{Binding SortMangaCommand}" Grid.Row="1" Width="Auto"/>
                    <Button Content="Очистить" Command="{Binding CancelFiltersCommnad}" Grid.Row="2" Width="Auto"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
