<mah:MetroWindow x:Class="ReadMangaApp.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReadMangaApp.View"
        mc:Ignorable="d"
        Title=" " Height="450" Width="700" TitleBarHeight="30" NonActiveWindowTitleBrush="LightGray" 
        BorderBrush="Gray" WindowTitleBrush="Gray"
        WindowStartupLocation="CenterScreen">
    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Border BorderThickness="1" CornerRadius="5" Padding="35,15" Background="White" Grid.RowSpan="5" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,15">
            <StackPanel>
                <Label Content="Вход" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="20" Margin="0,0,0,20" />
                <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                    <Label Content="Логин:" Margin="0,5,0,0"/>
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" Width="295"/>
                </StackPanel>
                <StackPanel Grid.Row="3" HorizontalAlignment="Center">
                    <Label Content="Пароль:" Margin="0,5,0,0"/>
                    <PasswordBox x:Name="PasswordBox" Width="295" PasswordChanged="PasswordBox_PasswordChanged"/>

                </StackPanel>
                <WrapPanel Grid.Row="4" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="Регистрация" Margin="0,0,5,0" Width="90" BorderBrush="{x:Null}" Command="{Binding OpenRegistrationWindowCommand}"/>
                    <Button Content="Войти" Command="{Binding LoginCommand}" Width="90" Background="LightGray" Margin="0,0,5,0"/>
                </WrapPanel>
            </StackPanel>
        </Border>
    </Grid>
</mah:MetroWindow>
