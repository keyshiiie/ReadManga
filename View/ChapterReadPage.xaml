<Page x:Class="ReadMangaApp.View.ChapterReadPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReadMangaApp.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChapterReadPage">

    <Grid Background="White">
        <TextBlock Text="{Binding CurrentPage.PageNumber, StringFormat='Номер страницы: {0}'}" 
                   FontSize="14" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        <ComboBox ItemsSource="{Binding Chapters}" 
                  SelectedItem="{Binding SelectedChapter}" 
                  DisplayMemberPath="ChapterTitle" 
                  Width="200" 
                  Margin="5,40,5,5" 
                  HorizontalAlignment="Left" 
                  VerticalAlignment="Top"/>

        <Button Content="Назад" Command="{Binding GoBackCommand}" Width="100" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <Viewbox Stretch="Uniform">
            <Border Background="Gray" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <Image Source="{Binding CurrentPage.ContentUrl}" Stretch="Uniform"/>
            </Border>
        </Viewbox>

        <Button Content="Вперед" Command="{Binding GoForwardCommand}" Width="100" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    </Grid>
</Page>
